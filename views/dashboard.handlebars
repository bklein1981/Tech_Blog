<main>
    <div class="navbar custom-brown">
        <div class="navbar-brand">
            <div class="nav-badge px-3 lrg-font">My Dashboard</div>
        </div>
    </div>
</main>
<section class="container-fluid py-5">
    <div class="row justify-content-center">
        {{#each posts as |post| }}
        <div class="col-10">
            <div class="custom-gray-bg custom-gray radius sml-font p-3">{{post.title}}<span class="float-r">{{format_date post.date_created}}</span></div>
        </div>
        {{/each}}
    </div>
</section>
<div class="container-fluid custom-grn-bg text-center pb-3">
    <div class="row align-items-end">
        <div>
            <button type="button" class="btn btn-lg custom-brown custom-green med-font radius15">+ New Post</button>
        </div>
    </div>
</div>