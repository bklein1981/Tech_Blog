<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tech Blog</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Open+Sans:wght@300;700&display=swap" rel="stylesheet">
</head>

<body>
    <header class="navbar custom-brown">
        <nav class="container-fluid pb-3">
            <ul class="navbar-nav flex-row nav-margin">
                <a href="/" class="btn nav-btn nav-padding">Home</a>
                <a href="/dashboard" class="btn nav-btn nav-padding">Dashboard</a>
                {{#if logged_in}}
                <a class="hidden-button" id="login">Login</a>
                <a class="btn nav-btn nav-padding" id="logout">Logout</a>
                {{else}}
                <a href="/login" class="btn nav-btn nav-padding">Login</a>
                <a class="hidden-button" id="logout">Logout</a>
                {{/if}}
            </ul>
        </nav>
    </header>
    <section id="main">
        {{{ body }}}
    </section>
</body>
{{#if logged_in}}
<script src="/js/logout.js"></script>
{{/if}}

</html>